# this is workflow name 
name :  Hello Alekh
# runs on trigger

on : 
  push :
     branches: [main]
# these are jobs inside the workflowa
jobs:
  #this is the name of job
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to users
        run: echo "Hello dosto"

      - name: say hello to username
        run: echo "Hello $USER"
